server {
    listen ${NGINX_PORT};
    location /graphql {
        proxy_pass http://${GRAPH_QL_HOSTNAME}:${GRAPH_QL_PORT};
        resolver 127.0.0.11;
        # Add CORS headers here if needed
    }
    location / {
        root ${NGINX_FOLDER};
        index index.html;
    }   
}