{
    "name": "Generator",
    "version": "1.0.0",
    "scripts": {
      "deploy": "tsc && node --trace-warnings build/main.js >> $LOG_MOUNT/generator.log 2>&1",
      "test": "tsc && node build/test.js"
    },
    "private": true,
    "license": "UNLICENSED",
    "type": "module",
    "prettier": {
      "printWidth": 80
    }
  }
