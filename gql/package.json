{
    "name": "graphql",
    "version": "1.0.0",
    "scripts": {
        "deploy": "tsc; node build/main.js >> $LOG_MOUNT/gql.log 2>&1",
        "test": "tsc; mocha build/test.js --exit"

    },
    "private": true,
    "license": "UNLICENSED",
    "type": "module",
    "prettier": {
        "printWidth": 80
    }
}
