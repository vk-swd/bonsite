# Grafana Analytics Dashboard


A Grafana service that uses [Prometheus](./Prometheus.md) as datasource and hosted as a webpage with login turned off. The dashboard layout is also prepared to visualise most metrics currently reported by services.